import * as React from "react";

/**
 * This component renders the 404 error page when an unknown route is used.
 */
const NotAuthorized = () => {
  return (
    <div>
      <h1>401</h1>
      <h2>Not Authorized!</h2>
    </div>
  );
};

export default NotAuthorized;
